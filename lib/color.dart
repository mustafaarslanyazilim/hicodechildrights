import 'package:flutter/material.dart';

class AppGradients {
  static const LinearGradient primaryGradient = LinearGradient(
    colors: [
      Color(0xFF8142EF), // Başlangıç reng
      Color(0xFFB566F2), // Bitiş reng
    ],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
}

class AppColors {
  // Primary colors
  static const Color purple = Color(0xFF8142EF); 
}
